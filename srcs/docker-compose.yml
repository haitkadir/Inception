services:
  db:
    # image: mariadb
    build: ./requirements/mariadb
    container_name: mariadb_c
    volumes:
      - mariadb_db:/var/lib/mysql
    env_file:
      - ./.env
    networks:
      - np
    restart: always

  wordpress:
    build: ./requirements/wordpress
    container_name: wordpress_c
    volumes:
      - wordpress_db:/var/www/html
    depends_on:
      - db
    env_file:
      - ./.env
    networks:
      - np
    restart: always
  
  nginx:
    build: ./requirements/nginx
    container_name: nginx_c
    depends_on:
      - wordpress
    volumes:
      - wordpress_db:/var/www/html
    ports:
      - "443:443"
    restart: always
    networks:
      - np

volumes:
    mariadb_db:
      driver_opts:
        type: none
        o: bind
        device: /home/ubuntu/Desktop/Inception/srcs/requirements/mariadb/db     
    wordpress_db:
      driver_opts:
        type: none
        o: bind
        device: /home/ubuntu/Desktop/Inception/srcs/requirements/wordpress/data
networks:
      np:
        driver: bridge
 
 
